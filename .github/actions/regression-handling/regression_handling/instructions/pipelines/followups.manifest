# Optional extra Copilot passes after the main pass. One entry per line:
#   <trigger_function> <instruction_path>
# Use any whitespace between trigger and path (spaces/tabs). Path is the rest of the line (trimmed).
# trigger_function must exist (sourced from regression_handling.sh). Path is relative to regression_handling root.

should_run_hang_followup_analysis instructions/hang_stage_instructions_for_llm.txt
