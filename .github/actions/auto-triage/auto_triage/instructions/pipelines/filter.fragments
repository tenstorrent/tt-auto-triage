# Concatenated in order for the filter-stage Copilot pass (one path per line, relative to auto_triage root).
# Add a new line to append another instruction fragment (e.g. filter_perf_instructions_for_llm.txt).

instructions/filter_instructions_for_llm.txt
instructions/filter_hang_instructions_for_llm.txt
