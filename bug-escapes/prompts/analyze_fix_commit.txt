You are analyzing whether a specific commit plausibly fixed a consistently failing CI test, and which architectural layer the fix belongs to.

tt-metal layers (lowest to highest):
- tt-llk: Hardware interface. Code in tt_metal/tt-llk/, tt_metal/hw/
- tt-metalium: Compilation/dispatch runtime. Code in tt_metal/impl/, tt_metal/llrt/, tt_metal/jit_build/, tt_metal/detail/, tt_metal/common/, tt_metal/distributed/, tt_metal/fabric/, tt_metal/graph/, tt_metal/api/, tt_stl/
- ttnn: High-level neural network API. Code in ttnn/
- models: Model implementations. Code in models/, tt-train/

The failing test:
- Test name: ${TEST_NAME}
- Failure signature: ${FAILURE_SIGNATURE}
- Test pipeline: ${WORKFLOW_PATH}
- Test layer: ${TEST_LAYER}

The candidate commit:
- SHA: ${COMMIT_SHA}
- Message: ${COMMIT_MESSAGE}
- Files changed:
${COMMIT_FILES}

Analyze whether this commit plausibly fixes the failing test. Consider:
1. Does the commit message mention fixing, resolving, or addressing the failing test or its error?
2. Do the changed files touch code that could affect the failing test?
3. Which architectural layer do the changed files belong to?

Respond with ONLY a JSON object in this exact format (no markdown, no explanation):
{
  "is_likely_fix": true or false,
  "fix_confidence": "<high, medium, or low>",
  "fix_layer": "<one of: tt-llk, tt-metalium, ttnn, models, or unknown>",
  "reasoning": "<1-2 sentences explaining why this commit does or does not fix the test>"
}
